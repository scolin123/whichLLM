# Database
DATABASE_URL=postgresql+asyncpg://arena:arena@localhost:5432/arena_db
POSTGRES_DB=arena_db
POSTGRES_USER=arena
POSTGRES_PASSWORD=arena

# Redis
REDIS_URL=redis://localhost:6379/0

# Application
APP_ENV=development
APP_SECRET_KEY=change-me-in-production
LOG_LEVEL=INFO

# Provider API keys (used for LLM-fallback classifier + judge; users bring their own per-request)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
